name: Universal Performance ARM64 Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  actions: write

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Android SDK
        uses: android-actions/setup-android@v3

      - name: Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Verify Gradle Wrapper
        run: |
          echo "Checking gradle wrapper files..."
          test -f gradle/wrapper/gradle-wrapper.jar && echo "✓ gradle-wrapper.jar found" || echo "⚠ gradle-wrapper.jar not found - will be regenerated"
          test -f gradle/wrapper/gradle-wrapper.properties && echo "✓ gradle-wrapper.properties found" || echo "✗ gradle-wrapper.properties missing"
          test -f ./gradlew && echo "✓ gradlew found" || echo "✗ gradlew missing"

      - name: Regenerate Gradle Wrapper
        run: |
          # Download and regenerate gradle wrapper if jar is missing
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "Regenerating gradle wrapper..."
            # Use a temporary gradle installation to regenerate wrapper
            gradle wrapper --gradle-version 8.5 --distribution-type bin 2>/dev/null || true
            chmod +x ./gradlew
          fi

      - name: Clean
        run: ./gradlew clean

      - name: Build ARM64 release
        run: ./gradlew :app:assembleRelease --no-daemon

      - name: Verify APK
        run: |
          ls -lh app/build/outputs/apk/release/
          file app/build/outputs/apk/release/*.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: universal-performance-arm64
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error

      - name: GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: app/build/outputs/apk/release/*.apk
          
